--breakpoints = {
  xs: 0px,
  sm: 540px,
  md: 768px,
  lg: 1440px
};

--grid-columns = 8;
--max-width = 1440px;

--gutters = {
  xs: 10px,
  md: 13px,
  lg: 21px
};

--outer-margins = {
  xs: 20px,
  md: 30px,
  lg: 40px
};
